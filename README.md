# Compound-Tile-CSS

This repo exists to host CSS files for Hubitat. The app Compound Tile uses CSS to make multiple component tiles from a virtual button controller look like a single contiguous tile. These files are imported into dashboards along with a locally stored copy. Broadly, content of the tiles is set via HTML attribute, appearance of the tiles themselves (background color, etc.) is set by CSS.
