# Compound-Tile-CSS

This repo exists to host css files for Hubitat. The app Compound Tile uses CSS to make component tiles from a virutal button controller look like a single contiguous tile. These files are imported into dashboards along with a locally stored copy. 